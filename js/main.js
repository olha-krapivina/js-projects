"use strict";

(function (){
    const container = document.querySelector("#container");

    // const createCardStructure = 

    const render = () => {
        const blocks = notes.map((card) => {
            const cardBlock = document.createElement("div");
            cardBlock.className = "card";
        }); 
    };
    render();
})();